import React, { Component } from 'react';
import {
  View,
} from 'react-native';

import { connect } from 'react-redux';

export class ObjectInfoView extends React.Component {
  render() {
    return <View>
    </View>
  }
}

export default connect(null, null) (ObjectInfoView);
